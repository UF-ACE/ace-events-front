apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-front
  namespace: ace-official
  labels:
    app: events
    tier: frontend
    service: web
spec:
  replicas: 2
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      labels:
        app: home
        service: web
    spec:
      containers:
        - name: events-front
          image: quay.io/uface/event-front:0.1.0
          ports:
            - containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 3
          lifecycle:
            preStop:
              exec:
                command: ["/usr/sbin/nginx","-s","quit"]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-back
  namespace: ace-official
  labels:
    app: events
    tier: backend
    service: web
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      labels:
        app: home
        service: web
    spec:
      containers:
        - name: events-back
          image: quay.io/uface/event-back:0.1.0
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: PG_HOST
              valueFrom:
                secretKeyRef:
                  name: events
                  key: host
            - name: PG_DATABASE
              valueFrom:
                secretKeyRef:
                  name: events
                  key: database
            - name: PG_USERNAME
              valueFrom:
                secretKeyRef:
                  name: events
                  key: username
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: events
                  key: password
      initContainers:
        - name: events-db-init
          image: quay.io/uface/event-db-init:0.1.0
          env:
            - name: PG_HOST
              valueFrom:
                secretKeyRef:
                  name: events
                  key: host
            - name: PG_DATABASE
              valueFrom:
                secretKeyRef:
                  name: events
                  key: database
            - name: PG_USERNAME
              valueFrom:
                secretKeyRef:
                  name: events
                  key: username
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: events
                  key: password
          
---
apiVersion: v1
kind: Service
metadata:
  name: events-front
  namespace: ace-official
  labels:
    service: web
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: events
    tier: frontend
---
apiVersion: v1
kind: Service
metadata:
  name: events-back
  namespace: ace-official
  labels:
    service: web
spec:
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: events
    tier: backend
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: events
  namespace: ace-official
  labels:
    service: web
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - uf-ace.com
        - www.uf-ace.com
      secretName: home-tls
  rules:
    - host: uf-ace.com
      http:
        paths:
          - path: /events/
            backend:
              serviceName: events-front
              servicePort: 80
          - path: /events/auth
            backend:
              serviceName: events-back
              servicePort: 80
          - path: /events/api
              backend:
                serviceName: events-back
                servicePort: 80
    - host: www.uf-ace.com
      http:
        paths:
          - path: /events/
            backend:
              serviceName: events-front
              servicePort: 80
          - path: /events/auth
            backend:
              serviceName: events-back
              servicePort: 80
          - path: /events/api
              backend:
                serviceName: events-back
                servicePort: 80
